# homebrew-keepresso

Homebrew Cask tap for [Keepresso](https://github.com/gyorgysh/keepresso), a
macOS menu-bar keep-awake app.

## Install

```sh
brew install --cask gyorgysh/keepresso/keepresso
```

Keepresso updates itself after that via its built-in Sparkle auto-updater.
